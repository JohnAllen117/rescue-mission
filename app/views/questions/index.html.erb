 <h1>THIS IS MO'FUCKIN' QUESTIONS</h1>

<% @questions.each do |question| %>
  <h2><a href="/questions/<%= question.id %>"><%= question.title %></a></h2>
  <a><%= question.body %></a>
  <%= User.where(id: question.user_id).first.name %>
<% end %>

