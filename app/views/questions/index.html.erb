<h1>Rescue Mission</h1>
<% @questions.each do |question| %>
  <h2><a href="/questions/<%= question.id %>"><%= question.title %></a></h2>
  by - <%= User.where(id: question.user_id).first.nickname %>
<% end %>

